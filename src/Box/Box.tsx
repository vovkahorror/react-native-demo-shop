import {View} from 'react-native';

export const Box = () => {
    return (
        <View>

        </View>
    );
};

interface BoxPropsType {
    bgr: string;
    width: number;
    height?: number;
}